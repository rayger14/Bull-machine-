[pytest]
testpaths = tests
addopts = -q -ra --strict-markers

markers =
    legacy: legacy API tests pending migration
    golden: tests using golden fixtures
    slow: slow-running tests (>1s)
    integration: integration tests
    v170: v1.7.0 integration tests (legacy)