[bumpversion]
current_version = 1.4.2
commit = True
tag = True
tag_name = v{new_version}
message = Bump version: {current_version} → {new_version}

[bumpversion:file:bull_machine/version.py]
search = __version__ = "{current_version}"
replace = __version__ = "{new_version}"

[bumpversion:file:README.md.j2]
search = {{ version }}
replace = {{ version }}

# Note: README.md will be auto-regenerated by scripts/render_readme.py
# after the version bump