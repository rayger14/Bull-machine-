# Isolated conditional-entry development comparison

User approved actual code-versus-agent historical comparison, with independent
archetypes and equal hourly/minute standing. Quant advisory approved the bounded
four-case design below. This is development research, not a full walk-forward,
CPCV, training, profitability or live-readiness certificate.

## Source restoration and population

Reconstruct the unchanged April `[2026-04-01,2026-05-01)` cohort, seed
`2026-03-02T00:00Z`, with the original pilot selector, parent policies and SPEC
identity. The temporary original archive disappeared; restoration is accepted
only at its original SHA256
`5b8a4533f70b8ccd0dc533984469e6886ec73ce315d48f150c667ccb97e84035`.
Keep recovered bytes in permanent project storage with recovery provenance.
Do not substitute the different local Databento futures file.

Capture all original candidate envelopes before the old stratum selector,
preserving its unchanged return. Reconcile the regenerated source month,
245-entry ledger, counts and three original selected evidence payloads exactly.
Unselected full evidence is newly regenerated under locked inputs, not compared
with nonexistent old full payloads. These are three hourly LC structural/numeric
and 242 minute equal-low sweep/reclaim candidates, not all seventeen populations.
Preserve old scripts, artifacts, assessments, grades and outcomes.

## Fixed selection and calls

Select the first two previously unassessed candidates per track by
`(decision_time,candidate_id)`. Exclude identities in the old April hidden-controls
metadata and earlier prior-assessed-case registry. Preserve all245 rows with
selected/exclusion reasons. Do not select on H3, native emission, plan validity,
future performance or model response. Shortages remain shortages; no replacement,
retry or answer repair. April is exposed development data even for new case IDs.

At most four fresh assessors and four independent fresh reviewers, requested
`gpt-5.6-sol`, high, consistent with the prior bounded pilot. Actual snapshot and
billed tokens are unobserved unless tools supply them. Source-only quant advisory
is not a market assessor. A mechanically invalid assessment is locked as failed;
do not manufacture a bound review request or spend a replacement call.

## Evidence and binding

Use existing source-only lower packet builder for completed
1m/5m/15m/1h/4h/daily candles, witnessed sequence, causal parent views and indicative
economics. Include master-authored specialist teaching/rulecard text without
known educational examples, prior answers or future outcomes. Add the hourly
native diagnostic as as-of evidence so gates/fusion/cooldown/pre-winner eligibility
are inspectable. Receipt uncertainty and missing macro/derivatives remain explicit.

New `research_identity` binds archetype, track, variant and source candidate ID
before catalog/menu compilation. Use unchanged conditional assessor/reviewer
builders, evidence-ID contracts and silo wrapper. Capture actual inner chunk
read returns in order before emission; validate hashes/content and role/case
binding. Chunk capture does not certify attention or enforced filesystem isolation.
Fresh roles receive only their case request and may not inspect other files,
future prices, prior cases or use tools except the exact receipt reads/writes.

Lock protocol, source, selection, packets, menus, settings, code and prompts
before assessment. Lock raw response and receipts before review construction;
lock review/receipts and deterministic grades before outcome reveal. No grade or
answer changes after reveal. Invalid receipts, null choices and failed reviews
retain unavailable plan/PnL, not rejection or avoided-loss credit.

## Frozen reference arms and execution

Every archetype × track × arm is a separate book, no cross-archetype capacity,
capital pool or combined portfolio PnL. Arms on the same selected candidates:

1. `cohort_enter`: unconditional immediate long for each eligible research cohort
   candidate. This is broader than hourly native emission, not the full engine.
2. `fixed_wait_5m_high`: wait for a subsequent eligible 1m close above the latest
   completed 5m high using the unchanged menu policy. Missing anchor is unavailable,
   not a substituted trigger.
3. `native_pre_winner` (hourly): immediate long only if the captured pre-dedup
   `native_diagnostic.native_signal` is long; otherwise no order. Post-winner
   `selected`/`native_emitted` is descriptive only. Minute `cohort_enter` is the
   separate minute-detector reference, not a native minute champion engine.
4. `agent_conditional`: validated enter/reject/wait choice from the sealed menu.

All arms use 90-second assumed processing, zero routing delay and 15-minute
exclusive entry expiry, fixed original stop, actual-entry-derived 2R target,
original absolute deadline (1440 minutes hourly,240 minute), $50000 fixed
notional and12bps round-trip cost ($60). These are common research brackets,
not native engine management parity or measured model latency. Starting equity
unspecified; unfunded, no funding/impact/inference costs or certified fills.
Use previously reviewed causal gap/stop-first/deadline and occupancy policies.

After all role records and grades are locked, also evaluate the same fixed code
arms over the full regenerated cohort, reporting hourly/minute separately. This
full-cohort code reference is not a full-population agent comparison. Selected
case books are fresh sample books, not the full-month book with other trades
retroactively filtered out. No parameter search or best-arm selection.

## Report and stop rule

Report selected-case dates, raw plan IDs, contract/receipt/reviewer validity,
validated admissions/waits/rejections and matched reference outcomes. Show net
PnL after assumed costs, entered count, initial risk and wins/losses; distinguish
known subtotal from incomplete-policy null. List selected-case winners missed
or losses avoided only when a validated alternative justifies that description.
An ungated raw-choice economic replay may be shown only as explicitly labeled
diagnostic, never validated agent performance. Do not add rejected/no-fill cases
to the win-rate denominator. No calibrated probability claims.

Full-cohort code results are separately labeled. Four-case agent results cannot
establish expectancy, superiority or holdout performance. Conclude with the
observed disagreement and failure modes, then specify the next prospective
chronological evaluation; do not turn this unit into another synthetic scale-up.
No live/config/fusion/archetype changes, new dependencies, push or PR.
