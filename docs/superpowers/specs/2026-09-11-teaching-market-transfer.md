# Teaching-on/off real-market transfer — frozen v1

User requests agents understand saved trader teachings and actual code, then test
that understanding. Quant guide approved advancing after both local-corpus
comprehension assessors passed. Research only; no production or live authority.

## Selection before market outcomes

Use August2026, the next complete month after the March/May/July assessment
schedule. The existing Binance1m archive ends August31 23:59UTC and has complete
July2–September1-exclusive coverage, including the exact30-day seed before
August1. Confirm actual source hash/grid before replay. This is an unassessed
calendar month for this paired study, not a pristine research holdout.

Reuse the frozen prior `prepare_sources.py` by importing it in a separate private
wrapper, changing only output directory, specification path and selected month.
Do not edit the frozen prior driver or artifacts. Hash the wrapper and imported
driver. Run exactly one guarded, source-only month reconstruction: complete
same-stream hours, actual LFC and all17 native signal archetypes, no native book,
allocation, simulator or model assessment. Preserve original30-day cold start,
constructor IPv6-probe convention and side-effect guards. Record missing external
observations/fallbacks and retain errors; no feed fabrication or source splicing.

Select first candidate by decision time within [Aug1,Aug15) and [Aug15,Sep1),
separately for hourly LC and the existing causal minute sweep/reclaim detector.
Hourly selection is unchanged: structural identity plus finite volumeZ>=3,
RSI>65 or<35, BBwidth<=.06, chop<=.5 and previous completed hour BBwidth<=.06.
Do not condition on native emission. Minute selection is unchanged detector
chronology, tie order and spacing with the original seed; no parent filter.
At most two hourly and two minute cases. Missing strata stay absent; invalid
plans or unavailable scoring tails stay reported, never replaced by outcome.

## Shared evidence and paired arms

Completed same-source layers:30×1m,12×5m,16×15m,24×1h,18×4h,10×1d. Reuse the
reviewed strict complete-bucket helper and prefix-recomputed minute witness.
Use fixed4H/N3 and1D/N3 parent ledgers bound strictly before setup, plus updates
only through decision. Verified parent absence differs from missing coverage.
These anchors and lower-half geometry are project hypotheses, not teacher rules.

Both arms get identical market evidence, exact track-specific code/rulecards,
scope caveats, decision questions and output budget. OFF gets no teaching-note
excerpts. ON additionally gets the seven secondary-note excerpts from the frozen
comprehension packet, unmodified, including conflicting HTF-size descriptions
and provenance limitations. Candidate contracts H1/H2/H3 are shared project
context in both arms, not part of the treatment. Do not give either arm prior
assessment answers, hidden native/H3 masks, future data, or performance comments.
The tested treatment is this excerpt package, not a generic claim about agents
or all trader knowledge. ON necessarily has extra context tokens; output
allowance is equal, total compute cost is not certified equal.

One fresh context per case per arm: maximum8 calls, fixed order by case ID then
OFF/ON. No shared context between cases, paired response sharing or retries.
Each response at most1000words, with:

- `facts`: as-of candle completeness; applicable detector/identity and gate
  evidence; pre-setup4H/daily parent presence and bounds if present; distinction
  between current event and complete higher-level model. Supply exact evidence
  IDs/values/timestamps for claims, not generic all-source citation lists.
- `sequence`: larger structure, local location, event order, observed confirmation
  versus inferred model interpretation, and specific missing evidence. Do not
  make MSS/LPS/RTZ a universal gate for every native archetype merely because it
  appears in an illustrative teaching sequence.
- `decision`: accept/reject/insufficient_evidence for the unchanged immediate
  hypothetical entry, with the decisive reason. No replacement stop or entry.
- `probability_net_positive`: number0..1 or null with explanation if unassessable.
  This forecast concerns the complete fixed net-payoff contract, including
  deadline exits—not merely whether a2R target is reached.
- `citations`: individual material claims mapped to exact provided evidence IDs.

No live receipt certificate is required for a conditional research judgment;
missing macro/derivative/weekly/monthly structures must not be invented. An
observed event need not establish a complete discretionary teaching model.

## Primary evaluation, locked before calls

Build an outcome-hidden factual reference from input values/code and independent
checks before assessment. Common criteria, identically applied to both arms:

1. Correct applicable native identity/gate or minute detector statements; missing
   native-gate evidence is not converted into observed confirmation.
2. Correct decision/availability chronology, no developing-bar or later-parent
   authorization; distinguish verified absent parent from unverifiable evidence.
3. Numerical claims about bounds, pivot/sweep/reclaim levels and fixed plan agree
   with cited inputs at stated rounding precision. Approximate target may use
   indicative close only if explicitly labeled; actual entry is withheld.
4. No invented feed observation, pivot/parent identity, confirmation or full
   model completion presented as measured fact. Unsupported discretionary labels
   remain interpretations, not false precision or evaluator-created gold truth.
5. No universal-H3/HTF veto, LC/minute rule mixing, predicate-pass-as-executed-trade,
   or unsupported teacher attribution. OFF's lack of teaching citations is not
   itself an error. ON must not silently resolve the supplied corpus conflict.

Report each criterion per response as pass/fail/not_assessable with exact claim
evidence; omissions separately from wrong claims. Independent quant review
adjudicates qualitative entailment without economic outcomes, neither majority
vote nor a fitted weighted score. Do not infer treatment value from acceptance
frequency or one more fluent narrative. This is a tiny descriptive paired probe.

## Secondary frozen economic diagnostic

Before any call, freeze the same prior `entry_case_outcome.score_case` contract:
same-source decision-minute OPEN, zero added model delay, fixed independent
$50,000 notional/$60 round-trip cost, unspecified equity. Hourly stop=setup-close
minus2.7nativeATR,24hour horizon; minute stop=sweepminimum*.9985,4hour horizon.
Target=entry+2×(entry−stop). Opening gaps take precedence, otherwise both-touch
stop-first/ambiguous, deadline OPEN only. Invalid stop>=entry has no bracketPnL;
missing complete tail is unscorable, no replacement. No funding, impact,
compounding, model costs or certified obtainable fills. MFE/MAE full horizon,
including after exit. No agent-selected management or conditional-entry execution.

Lock all responses, complete factual review, then reveal all counterfactuals
including rejected cases. Show pertrack/arm coverage and errors, decisions,
probabilities and descriptive Brier error against netPnL>0 where forecast and
valid outcome exist. Null forecasts retain their denominator separately.
Compare hourly native emission/numeric selection; minute detector/fixed4H-N3H3;
and always-skip. Show case and policy initial-risk denominators, not portfolio
Sharpe or significance. No cutoff tuning, profitable-agent claim or model
promotion from four paired cases. Economic outcomes cannot retroactively change
factual grading, source interpretation or case inclusion.

On completion, quant guide reviews the evidence for the next bounded step.
Neither a quiz pass nor this paired probe permits deployment. Freeze/preserve
every artifact and failure; do not retry revealed cases as independent evidence.
