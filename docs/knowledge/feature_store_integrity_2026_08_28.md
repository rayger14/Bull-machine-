# Feature-Store Integrity — chop_score formula drift + audit launch (2026-08-28)

**THE FINDING**: store `chop_score` was built with the DEPRECATED formula (1−ADX/100; store median 0.743) while live uses the recalibrated one (1−ADX/50; live median 0.541). Consumed-threshold impact: chop≤0.5 gates pass **5.9% of store bars vs 39.1% live (6.6x starvation)**; chop>0.45 tight-trailing triggers ~94% of store bars vs ~60% live (backtest exits systematically tighter than live). With volume_z era-drift (6.6%→12.1% pass) this explains LC's 15x fire-rate gap (24 backtest trades/5yr vs ~6/mo live). **LC's real record is its live+holdout one; the starved backtest was an artifact.** 4th witness-integrity bug of the campaign (taker snapshot, OI feed, CME roll, now store chop). A/B DELTAS from same-store comparisons mostly survive (bias cancels); ABSOLUTE baselines and live-vs-backtest comparisons carry the asterisk.

**Sweep-completion A/B (same day): REJECT.** range_position≤0.5 gate + hard mode on liquidity_sweep: archetype $73,505 (n=416, quietly the book's #2 earner) → $48,173; book −$47K, DD −22%. The 4 August top-of-range fires were anecdote; at scale, "sweeps" firing mid-trend are profitable dip-buys. Sweep keeps soft gates. (Ran on the contaminated store — revisit only if the store rebuild materially changes chop-gated flow.)

**IN FLIGHT**: full-store metrology audit (distribution parity at consumed thresholds, formula diffs, placeholder/frozen scan, consumption-ranked repair list) → then a store rebuild of defective columns with live formulas → full re-baseline → check which recent conclusions move.
