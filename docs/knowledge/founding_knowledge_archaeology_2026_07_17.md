# Founding Knowledge Archaeology — full inventory & survival audit (2026-07-17)

Two-agent archaeology of ALL trader knowledge injected since inception
(Sept 2025, v1.x era): Bojan, @Moneytaur, @Wyckoff_Insider, @IamZeroIka,
Crypto Chase, Phoenix. Deployed-path ground truth: coinbase_runner →
isolated_archetype_engine; hybrid_runner + bull_machine/ package = dormant.

## Survival summary
- **FULL (survived faithfully)**: Wyckoff 13-event engine, PTI trap index
  (IS the spring identity), fakeout intensity, BOMS, SMC v1.8 OB/FVG/BOS,
  ATR sizing.
- **PROXY (crude simplification in production — detail lost)**: Bojan candle
  logic → wick_trap (lost: 70% magnet tier, trap-reset body≥1.25×ATR +
  direction-flip sequencing, pHOB, .705/.786 zones, confluence bonuses);
  HOB → order_block_retest (lost: 5-component quality score, touch-count
  level strength, consolidation prereq, reaction speed, INSTITUTIONAL tier);
  sweeps → liquidity_sweep (lost: equal-cluster magnets, reclaim-speed-in-
  hours, golden-pocket location, displacement quality, TTL decay);
  premium/discount + fibs (only ×0.92 penalty survives); MTF sync (lost:
  6-candle leg rule, DL2 deviation veto, EQ-magnet veto); advanced exits
  (lost: UTAD rejection, spring-flip reversal, Moneytaur structure trailing,
  absorption-vs-distribution asymmetry); multi-layer fusion (floors/triad —
  low priority per Lesson #54).
- **DORMANT (code exists, nothing calls it)**: PO3 sweep→break sequencing,
  wick magnets as PROFIT TARGETS (7-day decay TP ladder), M1/M2 TF-scaled
  scoring, phase-C trap score, reclaim-speed bonuses, orderflow LCA (CVD
  slope divergence, 1/3 body-close BOS, trap-pump upgrade), negative VIP,
  dynamic setup-quality risk multipliers [0.5,2.0], knowledge_hooks.py
  bounded-delta framework (zero call sites — the most complete never-wired
  synthesis), CRT, hidden-fib level hierarchy, negative fibs, Gann/temporal,
  Fourier filter, event engine, narrative traps, macro pulse, Oracle.
- **EXTINCT (git only)**: v1.1.2 signal-TTL system, SMT layer, disabled
  v1.4.1 rules, tuning ladders, V161 SPY params.

## Ranked lost-fidelity list (re-implementation candidates, champion-anchored)
1. **Trap-reset bar spec** → wick_trap boost/sub-archetype: direction flip +
   body ≥1.25×ATR + opposite-wick >30% (strict subtype of current K).
2. **HOB 5-component quality score** → OBR/S9 sizing-boost material
   (INSTITUTIONAL ≥0.7 tier). S9 resurrection validates detector runs.
3. **Sweep reclaim-speed**: ≤2 bars → 0.8 / ≤5h + ≥1.25ATR + golden pocket
   → ×1.3 — time-quality axis absent from all current archetypes.
4. **Equal-highs/lows cluster magnets**: 3+ equal extremes within 0.1% in
   10 bars — no production feature counts level touches.
5. **PO3 sweep→break sequencing** (vol ≥1.4×) — manipulation ORDER, both
   champions capture only halves.
6. **Phase-C trap score**: shallow pullback <38.2% + vol <0.8× = penalty.
7. **Dynamic risk multipliers** — aligns with boosts-over-filters (Rule 8).
8. **Wick magnets as profit targets** (unswept wicks, 7-day decay).
9. **Absorption-vs-distribution exit asymmetry** (hold vs partial).
10. **Moneytaur structure trailing**: max(BE+0.5R, pivot −1×ATR).
11. CVD trap-pump/hidden divergence (needs CVD in store first).
12. Hidden-fib hierarchy + negative fib targets.
13. knowledge_hooks bounded-delta discipline (integration pattern for all above).

## Discipline for re-implementation
Original spec verbatim (no tuning) → pre-register → battery V15 (all windows
+ holdout) → CPCV → only then live. One candidate at a time. Prefer
boost/sizing framing over filters (Rules 7-10). Full agent report with exact
parameters preserved in the git history of this file's creation commit;
source modules: bull_machine/modules/, bull_machine/strategy/,
engine/liquidity/, engine/fusion/knowledge_hooks.py,
configs/v141/exits_config.json.
