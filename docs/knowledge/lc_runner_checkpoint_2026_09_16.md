# LC runner checkpoint — September16

## Final completion supersedes the partial-foundation status below

The complete versioned lifecycle is implemented in
`scripts/research/lc_judgment_execution.py`, preserving the frozen foundation.
Three real-job synthetic end-to-end tests passed in730.78s; root14 preparation/
foundation regressions passed. Independent scoped review closed all findings.
Capture, conditional critic, failure/grade finalization, all20-terminal locking,
reveal gating and observed-balance monitoring are now implemented.

Actual `judgment_v1/runtime_v2` was prepared, verified and reopened:20 jobs,
zero attempts/terminals, no authorization or outcomes. Root independently
verified the runtime lock and zero-call state. Runtime-lock SHA256:
`dcb3f4dc34b23afa727d1e684bc982fff7bdbc3b596f94e80c0fa838bebfcb8c`.
`software_readiness.json` records approval without authorizing spend.

Next is the paid historical assessment campaign, not more runner development.
Latest observed balance797.76123 at2026-09-16T20:40:58.165Z does not meet1900
start floor. Recheck fresh balance before launch; no silent budget override.
The lead must retain one live controller process while dispatching external
roles with existing tools; no network client is needed inside the runner.
See [launch runbook](lc_assessment_runbook_2026_09_16.md).
No empirical agent edge or campaign PnL has been measured.

Scope: execution harness for the frozen20-case LC sample; no new selection,
curriculum, assessments, outcomes, thresholds or source replay.

Preparation and evidence locks reopen without drift. All20 have known hourly,
last-two1m and last-two5m facts. Each packet has30 one-minute,12 five-minute,
16 fifteen-minute,24 hourly,18 four-hour and10 daily completed candles derived
from its12-day causal prefix. Agents see these summaries, not all17,280 minutes.

Frozen parent-range classification:4h intact5, broken_down8, absent7; daily
intact11, broken_down4, absent5. These are predecision descriptions, not outcome
findings or new gates. All20 remain. Zero specialist captures exist.

Latest local balance observation:1437.24982 credits at2026-09-16T06:35:12.244Z;
not a live billing query. Existing1900 launch floor remains.

## Runner acceptance

- New versioned runner/runtime namespace; preserve every frozen file.
- Verify cross-bound roster/evidence; clone exact requests into runtime jobs;
  separately freeze runtime dependencies.
- Persistent ledger per live process; genuine restart invalidates measured timing.
- Durable consumed reservation before launch; no retry after uncertain dispatch.
- Exact chunk/raw capture, conditional eligible critic, immutable terminals;
  all terminals before reveal.
- Maximum20 specialists/20 conditional critics, three active roles,600-second
  deadlines; no retries, replacement cases or repairs.
- Budget blocks paid reservation, not local preparation/status/synthetic tests.

Existing sampling_prepare worker owns implementation and focused tests;
independent review follows finished diff. No model calls during implementation.

Budget-control scope ruling: first deliver the bounded runner foundation
(runtime preparation, persistent ledger, fresh budget gate and no-retry
reservation/recovery). Full byte-capture, critic and terminal lifecycle remain
explicitly launch-blocking if not completed in this bounded step. Never label
the foundation launch-ready. Root verified the existing ledger's6 focused
timing/timeout/duplicate regressions; the timing defect is in controller reuse,
not in the underlying ledger's same-instance elapsed-time calculation.

## Completed foundation and actual checkpoint

New `scripts/research/lc_judgment_runner.py` verifies and clones20 exact requests,
holds one ledger and an exclusive owner lease, pins runtime dependencies, checks
a fresh five-minute1900-credit campaign-start observation, and provides synthetic
dry-run reservation/restart recovery. Real reservation fails before consuming a
slot. Closing a runner prevents reuse; upstream lock hashes are rechecked.
Independent scoped review accepted both implementation fixes. Its requested
stale-after-close regression now covers six stale operations after a new owner
acquires the lease, and confirms the new owner's attempts remain zero.

Root prepared, verified, closed and reopened the actual `judgment_v1/runtime_v1`:
20 jobs; specialist0/reviewer0 attempts; no active/recovery-required roles;
launch_ready=false and roles_enabled=false. Runtime-lock file SHA256:
`b7157403c61479b366be6d1565b431efc8f0d34c5212e3208a8597e746b1aefc`.
No authorization, reservation, market assessment or outcomes in the real run.

Root14 runner/sampling/preparation tests pass; worker25 runner/ledger tests
pass (counts overlap; not a full research-suite run).

Remaining implementation: byte/runtime-return capture, conditional critic
preparation/routing, grade/failure terminal finalization, all-terminal reveal
gate wiring, and post-start campaign-spend tracking. These are explicitly
unsupported, not silently delegated to the old consecutive-block controller.
Complete this lifecycle and one no-model end-to-end check before paid launch;
fresh sufficient budget or explicit user budget revision is also still required.
